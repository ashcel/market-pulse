"""New-listing screener — the plane that answers "what is Binance about to
list, and is any of it worth a look?".

Read `sources.py` for where the data comes from, `smc.listing_score` for how
it is ranked, and `app.worker.listings_pass` for what runs it.
"""
