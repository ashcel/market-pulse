"""Discovery-pattern read/evaluate API — currently just REACCUMULATION /
SECOND EXPANSION (`smc.reaccumulation`). Public, like `app.market`: this
serves provider market data + a deterministic research read, no user
content, so it carries no auth dependency.
"""
