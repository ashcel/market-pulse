# M0-T2 — Write EDR 0017 (product direction)

## Context

This is milestone M0 (Honesty pass & direction commit) of the Market Pulse trading intelligence platform. The repo is at `/home/ubuntu/code/personal/market-pulse`. Tasks M0-T1 (land in-flight work) and D-T0 (tool verification) are already complete.

The repo has 16 existing Engineering Decision Records (EDRs) under `docs/decisions/` — see `docs/decisions/0016-trend-transition-derivation.md` for the latest format. EDRs follow a standard format: `# EDR <number>: <title>`, Status/Scope/Depends-on headers, Problem section, the chosen rules, rejected alternatives, validation, and future extension points.

CLAUDE.md at the repo root has an Architecture section (lines 62–92) that documents the three-plane architecture. The new EDR should be linked from there.

## Task

Create `docs/decisions/0017-product-direction.md` (EDR 0017) documenting the five product-direction decisions from the 2026-07-14 audit. The EDR must cover:

1. **Product scope** — Market Pulse is a decision journal + intelligence brief + behavior review platform. AI is a complement (BYOK, grounded, labeled), not a source of truth per se.
2. **Engine stance** — The engine is a context instrument only, pending its 1.0.0 forward-test verdict. No verdict/trigger output is presented as proven edge.
3. **R-normalization rule** — R-multiples are shown only where a stop order is evidenced in the record. Otherwise display % returns + MAE/MFE.
4. **Key custody** — API keys stored server-side, AES-256-GCM encrypted at rest, encryption key from environment variable, read-only permission enforced at the exchange API level.
5. **TradFi approach** — TradFi markets are accessed via Binance TradFi tickers (not a separate broker), gated on M7-T1's instrument-semantics survey.

Reference the 2026-07-14 audit as the source of these decisions.

## Definition of Done

- EDR committed to `docs/decisions/0017-product-direction.md`
- EDR linked from CLAUDE.md architecture section (the reading / reference section, lines 62–92)
- EDR follows the existing EDR format (status, scope, problem, decisions, rejected alternatives, validation)
- Typecheck and lint pass
- `bunx vitest run` green
- No changes to engine semantics, no new packages, no modifications to existing EDRs

## Constraints (always include; copy, don't reference)

- Do NOT modify src/lib/engine decision/trigger semantics or ENGINE_VERSION.
- Do NOT read 1.0.0 shadow-record outcomes (record:report --integrity only).
- SSE only, no WebSocket server endpoints. No src/server imports in client code.
- Migrations: hand-written SQL, next number in src/server/db/migrations/.
- New tables user-scoped (user_id FK). No plaintext secrets in DB or logs.
- R metrics only where a stop order is evidenced; else % / MAE-MFE.
- Match existing code style; tests colocated *.test.ts; do not touch routeTree.gen.ts; do not add packages without flagging (24h supply guard).
- Do not commit. Leave changes in the working tree for review.

## Review notes from previous attempt

*None — first attempt.*