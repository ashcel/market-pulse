# M0-T1 — Land the in-flight work

## Context
Milestone M0 (Honesty pass & direction commit). Three feature commits landed on main before the milestone plan was set up:
- `feat(spike)`: spike detector with discovery integration
- `feat(eval-log)`: eval_log table and persistence layer
- `feat(notifications)`: notification stream, bell, and settings UI

The tree is clean, `npx vitest run` passes (53 files, 868 tests), `npx tsc --noEmit` passes.
But `npm run lint` reports 19 errors and 11 warnings in 4 files from these in-flight commits.

## Task
Fix all lint errors in the 4 affected files, then verify the full suite still passes:

### Files to fix:
1. **`src/server/db/eval-log.test.ts`** (5 prettier comma errors, 5 `no-explicit-any` errors)
2. **`src/server/db/eval-log.ts`** (2 prettier formatting errors, 4 `no-explicit-any` errors)
3. **`src/server/worker/eval-pass.test.ts`** (2 prettier whitespace errors)
4. **`src/server/worker/eval-pass.ts`** (1 prettier whitespace error)

### Specific issues in eval-log files:
- **`any` types**: Replace `{} as any`, `as any` casts, and `any` type annotations with proper typed interfaces. The `componentScores` field in `EvalLogInput` and `EvalLogRow` should be `Record<string, unknown>` or a more specific type. In the test file, cast with proper typed mocks instead of `as any`.
- **Prettier commas**: Missing trailing commas in object literals in test file.
- **Prettier whitespace**: Extra spaces or missing newlines at various positions — run `npx prettier --write` on these files but verify the result doesn't break anything.

## Definition of Done
- [ ] `npm run lint` passes with 0 errors and 0 warnings
- [ ] `npx vitest run` still passes (all 868+ tests green)
- [ ] `npx tsc --noEmit` still passes (0 errors)
- [ ] `git status` clean (only the 4 files changed, or fewer if some already fine)
- [ ] No changes to engine decision/trigger semantics
- [ ] No changes outside the 4 affected files

## Constraints
- Do NOT modify engine decision/trigger semantics or ENGINE_VERSION.
- Do NOT read 1.0.0 shadow-record outcomes.
- Do NOT modify any file outside the 4 listed above.
- Do NOT remove `as any` from test file `context: {} as any` AND `execution.risk: {} as any` AND `execution.analytics: {} as any` AND the `execution.hold` AND `structure: {} as any` — those are placeholders for test fixtures. Only fix the ones where a proper type is simple to add (like `componentScores: Record<string, unknown>` instead of `any` in the main source file).
- Actually, the TEST FILE should keep `as any` where the mock object would be onerous to fully type. Fix only the `componentScores` `any` in the SOURCE files.
- Do NOT commit. Leave changes in the working tree for review.
- Match existing code style.