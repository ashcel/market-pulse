# M0-T3 — Homepage reframe

## Context

Milestone M0 (Honesty pass & direction commit). Task M0-T2 (EDR 0017) is committed. The homepage at `src/routes/index.tsx` (732 lines) currently presents engine calls as "Today's Edge" and "Actionable Setups" which implies proven edge — contradicting EDR 0017's engine-as-context-instrument stance.

## Task

Reframe the dashboard homepage copy and component labels so no UI string implies proven edge. **Copy only + component reshuffle, no data-layer changes.** The existing `marketEdge` and `marketIntentOutlook` functions continue computing their values; only the labels, headings, and descriptive text change.

Specific changes in `src/routes/index.tsx`:

### 1. Remove "Today's Edge" framing

- **Line 417:** `CardEyebrow>Today&apos;s Edge</CardEyebrow>` → `CardEyebrow>Tape Overview</CardEyebrow>` (or similar neutral framing)
- **Line 83-86 (tour step):** `title: "Today's edge"` → `title: "Today's conditions"` and body text reframed to describe conditions rather than "edge"
- **Line 65:** `TAB_FOR_TOUR_TARGET` key `edge` → rename to `tape` or `conditions`
- **Line 82, 160, 414:** All `data-tour="edge"` → `data-tour="tape"`
- **Line 397-400 (comment):** Update to remove "edge" language, describe as conditions/tape overview
- The component function `EdgeStrip` and the `edge` variable (line 411) can keep their names internally — no forced rename of code identifiers, just display text

### 2. Remove "Actionable Setups" framing

- **Line 468:** `CardEyebrow>Actionable Setups</CardEyebrow>` → `CardEyebrow>Engine Reads</CardEyebrow>`
- **Line 469:** `Engine decisions · 1H bars` → `Forward test in progress · 1H bars`
- **Line 473:** `No actionable setups right now` → `No engine reads right now` (keep the "wait" semantics)
- **Line 93-96 (tour step):** `title: "Actionable setups"` → `title: "Engine reads"` and body text reframed to emphasize forward-test status
- **Line 67:** `TAB_FOR_TOUR_TARGET` key `setups` → rename to `engine-reads`
- **Line 456 (comment area above TopSetups function):** Add a comment noting this section shows engine reads clearly labeled as forward-test-only

### 3. Data-tour consistency

- Update all `data-tour="setups"` → `data-tour="engine-reads"` (lines 93, 466)
- Update tour step target references consistently
- The `TAB_FOR_TOUR_TARGET` map entries (lines 63-74) need their keys updated to match

### 4. Subtitle line

- **Line 138:** `Here is your market intelligence for today.` — this is already appropriate, keep as-is

### 5. Component reshuffle (optional)

If it improves clarity, rename `TopSetups` → `EngineReads` and `EdgeStrip` → `TapeOverview` (but NOT required — display labels are what matters for the DoD).

## Definition of Done

- No UI string on the homepage implies proven edge
- "Today's Edge" → neutral tape/conditions label
- "Actionable Setups" → "Engine reads" with "Forward test in progress" sublabel
- Tour steps updated to match
- `bunx vitest run` green
- `bunx tsc --noEmit` clean
- `bun run lint` — 0 errors (pre-existing warnings acceptable)
- No data-layer changes (no changes to `marketEdge`/`marketIntentOutlook` or any engine files)
- Note: screenshots cannot be captured headlessly; note in output the visual changes made

## Constraints (always include; copy, don't reference)

- Do NOT modify src/lib/engine decision/trigger semantics or ENGINE_VERSION.
- Do NOT read 1.0.0 shadow-record outcomes (record:report --integrity only).
- SSE only, no WebSocket server endpoints. No src/server imports in client code.
- Migrations: hand-written SQL, next number in src/server/db/migrations/.
- New tables user-scoped (user_id FK). No plaintext secrets in DB or logs.
- R metrics only where a stop order is evidenced; else % / MAE-MFE.
- Match existing code style; tests colocated *.test.ts; do not touch routeTree.gen.ts; do not add packages without flagging (24h supply guard).
- Do not commit. Leave changes in the working tree for review.
- No data-layer changes AT ALL: do not modify marketEdge, marketIntentOutlook, or any engine functions. Display text only.

## Review notes from previous attempt

*None — first attempt.*